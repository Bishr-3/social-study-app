# Complete Database Schema Setup

هذا الملف يحتوي على الـ Schema الكامل لقاعدة البيانات لتطبيق "فخورون بالإمارات".

## الجداول المضمنة:

### الجداول الأساسية:
- `posts` - مشاركات الطلاب
- `comments` - التعليقات على المشاركات
- `app_settings` - إعدادات التطبيق (مثل الإعجابات المتعددة)

### جداول الإعجابات:
- `post_likes_audit` - تتبع الإعجابات المفردة (منع التكرار)
- `post_likes` - الإعجابات المتعددة

### جداول الميزات الإبداعية:
- `notifications` - نظام الإشعارات
- `user_avatars` - أفاتار المستخدمين
- `creative_rooms` - الغرف الإبداعية
- `creative_signatures` - التوقيعات الإبداعية
- `personal_museums` - المتاحف الشخصية

### جداول أخرى:
- `advertise_requests` - طلبات الإعلانات

## كيفية التشغيل:

1. اذهب إلى لوحة تحكم Supabase
2. افتح **SQL Editor**
3. انسخ محتوى ملف `complete-database-schema.sql`
4. الصقه في المحرر واضغط **Run**

## الميزات المضمنة:

- ✅ Row Level Security (RLS) مفعل على جميع الجداول
- ✅ Policies للتحكم في الصلاحيات
- ✅ Functions للإعجابات والإشعارات
- ✅ Triggers للإشعارات التلقائية
- ✅ Indexes للأداء الأمثل
- ✅ Storage buckets للصور والفيديوهات والملفات
- ✅ View للإحصائيات

## البيانات التجريبية:

بعد تشغيل الـ Schema، يمكنك إضافة بيانات تجريبية باستخدام ملف `test-videos-data.sql` لاختبار الفيديوهات.