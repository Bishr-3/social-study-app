-- ============================================================
-- 🇦🇪 تنظيف قاعدة البيانات - العودة لنظام الإعجابات الحر
-- ============================================================

-- 1. حذف جدول تتبع الإعجابات (لم نعد بحاجة إليه)
DROP TABLE IF EXISTS public.post_likes_audit;

-- 2. الإبقاء على دالة increment_like_v2 لأنها مفيدة للمشرف (Admin)
-- لكننا سنجعلها متاحة للجميع للزيادة فقط كما في السابق
-- (اختياري: يمكنك حذفها وإعادة الدالة القديمة، لكن الجديدة أفضل كودياً)

-- ============================================================
-- ✅ Cleanup Script Ready
-- ============================================================
